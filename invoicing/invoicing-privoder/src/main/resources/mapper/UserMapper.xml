<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.invoicingprivoder.dao.UserDao">
    <select id="getUser" parameterType="User" resultType="User">
        SELECT u.id,u.userName,u.realName,u.`password`,
        r.id as roleId,
        r.`name` as roleName
        FROM users u
        INNER JOIN role_user ru on ru.userId=u.id
        INNER JOIN role r on r.id=ru.roleId
        where userName=#{userName} and password=#{password}
    </select>
</mapper>